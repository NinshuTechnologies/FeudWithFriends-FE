import React from 'react';
import {View, Text} from 'react-native';

const ScoreTime = (props)=> {
    return (
        <View>
            <Text>This is the score card component</Text>
        </View>
    )
}

export default ScoreTime;